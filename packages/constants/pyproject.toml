[project]
name = "shopflow-constants"
version = "0.1.0"
description = "Shared constants (RBAC roles/permissions, enums) for ShopFlow services"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["shopflow_constants*"]
